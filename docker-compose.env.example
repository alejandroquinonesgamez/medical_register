# Archivo de ejemplo para variables de entorno de Docker Compose
# Copia este archivo a .env en la raíz del proyecto

# Nombre del proyecto para evitar conflictos con caracteres especiales
COMPOSE_PROJECT_NAME=medical_register

# Desactivar BuildKit para evitar problemas de compatibilidad
COMPOSE_DOCKER_CLI_BUILD=0
DOCKER_BUILDKIT=0

# Storage backend: memory | sqlite | sqlcipher
# STORAGE_BACKEND=sqlite
# SQLITE_DB_PATH=/app/data/app.db
# SQLCIPHER_DB_PATH=/app/data/app_secure.db
# SQLCIPHER_KEY=
# PASSWORD_PEPPER=

